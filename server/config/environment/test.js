'use strict';


module.exports = {

  // Here we can specify test environment specific options
  // for this demo we're just going to assume we're running in development mode
  // with stubs for data

};
